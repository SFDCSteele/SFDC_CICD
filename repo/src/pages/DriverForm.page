<apex:page standardController="Driver__c"  extensions="DriverCreation" sidebar="false">
    
          <div id="newDriver" >
                <apex:form >
                <apex:pageMessages id="errors" />
                <apex:pageBlock title="Create Driver" mode="edit">
                    <apex:pageBlockSection columns="1">
                        <apex:inputField value="{! driver.Contact__c }"/>
                     </apex:pageBlockSection>
            
                    <apex:pageBlockButtons location="top">
                        <apex:commandButton action="{! saveDriver }" value="Save" />        
                   </apex:pageBlockButtons>
            
                    <apex:pageBlock id="hiddenBlock1" rendered="false"></apex:pageBlock>
            
                    </apex:pageBlock>
                </apex:form>
          </div>

</apex:page>